# Real-Time-Acceleration-for-Medical-Image-Processing
To design and implement a real-time medical imaging pipeline capable of acquiring, reconstructing, enhancing, and analyzing medical images using AI, while achieving low-latency and high-throughput performance on edge computing platforms.

medical-imaging-pipeline/
│
├── matlab/
│   ├── beamforming/
│   │   ├── us_beamform.m                  # Main DAS beamformer function
│   │   ├── us_beamform_entry.m            # GPU Coder entry-point wrapper
│   │   └── generate_cuda_beamform.m       # GPU Coder codegen script
│   ├── enhancement/
│   │   ├── enhance_image.m                # Speckle reduction + CLAHE
│   │   ├── enhance_image_entry.m          # GPU Coder entry-point wrapper
│   │   └── generate_cuda_enhance.m        # GPU Coder codegen script
│   └── ai_inference/
│       ├── train_cnn.m                    # CNN training script (BUSI dataset)
│       ├── export_model_onnx.m            # Export to ONNX for TensorRT
│       └── label_data.m                   # Medical Image Labeler setup
│
├── cuda_generated/                        # Auto-generated by GPU Coder (do not edit)
│   ├── beamform/
│   │   ├── us_beamform.cu
│   │   ├── us_beamform.h
│   │   └── CMakeLists.txt
│   └── enhance/
│       ├── enhance_image.cu
│       ├── enhance_image.h
│       └── CMakeLists.txt
│
├── holoscan_app/
│   ├── CMakeLists.txt                     # Top-level CMake
│   ├── main.cpp                           # Pipeline definition + app entry
│   ├── operators/
│   │   ├── data_source_op.hpp             # RF data source operator
│   │   ├── data_source_op.cpp
│   │   ├── beamform_op.hpp                # MATLAB CUDA beamform operator
│   │   ├── beamform_op.cpp
│   │   ├── enhance_op.hpp                 # MATLAB CUDA enhancement operator
│   │   ├── enhance_op.cpp
│   │   ├── inference_op.hpp               # TensorRT AI inference operator
│   │   ├── inference_op.cpp
│   │   └── viz_op.hpp                     # Holoviz display operator (header only)
│   └── config/
│       └── pipeline.yaml                  # Holoscan app config
│
├── data/
│   ├── download_busi.m                    # Script to download BUSI dataset
│   └── sample_rf_data.mat                 # Sample simulated RF data (small)
│
├── models/
│   └── lesion_detector.onnx               # Exported CNN model
│
├── docker/
│   └── Dockerfile                         # Based on nvcr.io/nvidia/holoscan
│
├── docs/
│   ├── pipeline_diagram.png
│   └── operator_flow.md
│
├── README.md
└── .gitignore

## Stack
- MATLAB GPU Coder → CUDA (beamforming + enhancement)
- NVIDIA Holoscan SDK (operator pipeline)
- TensorRT (AI inference)
- Holoviz (visualization)

## Steps
1. Generate CUDA code in MATLAB:
   - Run `matlab/beamforming/generate_cuda_beamform.m`
   - Run `matlab/enhancement/generate_cuda_enhance.m`
2. Train and export CNN:
   - Run `matlab/ai_inference/train_cnn.m`
   - Run `matlab/ai_inference/export_model_onnx.m`
3. Build Holoscan app:
```bash
   cd holoscan_app && mkdir build && cd build
   cmake -DBUILD_BEAMFORM=ON -DBUILD_ENHANCE=ON ..
   make -j$(nproc)
   ./medical_pipeline
```
4. Or use Docker:
```bash
   docker build -t med-pipeline . && docker run --gpus all med-pipeline
```

## Reference
- [HoloHub matlab_gpu_coder](https://github.com/nvidia-holoscan/holohub/tree/main/applications/matlab_gpu_coder)
- [BUSI Dataset](https://doi.org/10.1016/j.dib.2019.104863)
